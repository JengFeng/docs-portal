<?php
declare(strict_types=1);if(PHP_SAPI!=='cli'){http_response_code(403);exit;}require dirname(__DIR__).'/app/bootstrap.php';
try{$config=portal_config();$db=portal_open_database($config);if(!portal_document_version_blob_schema_ready($db))throw new RuntimeException('Migration 025 is not active.');$rows=$db->query("SELECT d.document_id,d.public_id,d.relative_path,d.file_name,d.extension,d.file_size_bytes,d.content_hash,d.current_version_id,d.version_capture_status,d.version_capture_error_code,v.source_content_hash current_version_hash,v.source_file_size_bytes version_size FROM dbo.documents d LEFT JOIN dbo.document_versions v ON v.version_id=d.current_version_id AND v.document_id=d.document_id WHERE d.status_code<>'archived' ORDER BY d.document_id")->fetchAll();$filesystemBytes=0;$sqlBytes=0;$bad=[];$streamDb=portal_open_sqlsrv_stream_database($config);
 try{foreach($rows as $row){$path=portal_document_file_path($config,$row);$fileSize=filesize($path);$fileHash=hash_file('sha256',$path);$expected=strtolower((string)$row['content_hash']);$filesystemBytes+=(int)$fileSize;$q=sqlsrv_query($streamDb,'EXEC dbo.portal_read_document_version_content @document_id=?,@content_hash=?',[(int)$row['document_id'],$expected],['QueryTimeout'=>600]);$ok=$q!==false&&sqlsrv_fetch($q)===true;$size=$stored=$sqlHash=$lob=null;if($ok){$size=(int)sqlsrv_get_field($q,0);$stored=(string)sqlsrv_get_field($q,1);$sqlHash=(string)sqlsrv_get_field($q,2);$lob=sqlsrv_get_field($q,3,SQLSRV_PHPTYPE_STREAM(SQLSRV_ENC_BINARY));$ok=is_resource($lob);}$ctx=hash_init('sha256');$count=0;if($ok){while(!feof($lob)){$chunk=fread($lob,1048576);if($chunk===false){$ok=false;break;}hash_update($ctx,$chunk);$count+=strlen($chunk);}fclose($lob);}$actual=$ok?hash_final($ctx):'';if($q!==false)sqlsrv_free_stmt($q);$sqlBytes+=$count;if((int)$fileSize!==(int)$row['file_size_bytes']||!is_string($fileHash)||!hash_equals($expected,strtolower($fileHash))||(int)$row['current_version_id']<=0||(string)$row['version_capture_status']!=='ready'||$row['version_capture_error_code']!==null||!hash_equals($expected,strtolower((string)$row['current_version_hash']))||(int)$row['version_size']!==(int)$fileSize||!$ok||$count!==$size||!hash_equals($expected,strtolower((string)$stored))||!hash_equals($expected,strtolower((string)$sqlHash))||!hash_equals($expected,$actual))$bad[]=(int)$row['document_id'];}}
 finally{sqlsrv_close($streamDb);} $verifyStatement=$db->query('EXEC dbo.portal_verify_document_version_contents');$verify=$verifyStatement->fetch();$missingIds=[];if($verifyStatement->nextRowset()){foreach($verifyStatement->fetchAll() as $missingRow)$missingIds[]=(int)$missingRow['version_id'];}$versionRows=(int)($verify['version_rows']??-1);$contentRows=(int)($verify['content_rows']??-1);$missingRows=(int)($verify['missing_rows']??-1);$invalidRows=(int)($verify['invalid_rows']??-1);$out=['database_name'=>(string)$db->query('SELECT DB_NAME()')->fetchColumn(),'active_documents'=>count($rows),'current_complete'=>count($rows)-count($bad),'bad_document_ids'=>$bad,'filesystem_bytes'=>$filesystemBytes,'current_sql_bytes'=>$sqlBytes,'version_rows'=>$versionRows,'content_rows'=>$contentRows,'missing_content_rows'=>$missingRows,'missing_version_ids'=>$missingIds,'all_version_content_bytes'=>(int)($verify['total_content_bytes']??0),'invalid_content_rows'=>$invalidRows,'passed'=>$bad===[]&&$filesystemBytes===$sqlBytes&&$versionRows===$contentRows&&$missingRows===0&&$missingIds===[]&&$invalidRows===0];echo json_encode($out,JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES|JSON_THROW_ON_ERROR),PHP_EOL;exit($out['passed']?0:2);
}catch(Throwable $e){fwrite(STDERR,'Document version content verification failed: '.$e->getMessage().PHP_EOL);exit(1);}
