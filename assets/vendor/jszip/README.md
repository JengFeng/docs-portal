# JSZip

This directory vendors the browser distribution of JSZip 3.10.1 from the
official npm package. TWWATER uses the MIT licensing option; see
`LICENSE.markdown`.

The file is served from the same origin and is used only to read authenticated
PPTX Open XML packages in the user's browser. No CDN is contacted.
